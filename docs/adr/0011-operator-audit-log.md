# Operator access is audited

Every Operator access writes an audit log (who, when, which Account or Session). v1 without a log was rejected: Operators may read other people's Sessions, and that fact must be reconstructible.

**Status**: accepted
