# Operators are configured emails

Who may administer the host is a list of Account emails in host configuration (environment), not “the first Account to register” and not a full admin UI in v1. First-registrant-wins is unsafe on a public sign-up page; a complete operator console can wait.

**Status**: accepted
