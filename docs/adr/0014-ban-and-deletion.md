# Ban keeps data; Deletion erases it

An Operator Ban stops sign-in and leaves Sessions, Workspaces, and Credentials in place for later audit. Account Deletion is self-service and erases that Account's data. Collapsing both into one action was rejected: a security Ban must not destroy evidence, and a person leaving must not need an Operator.

**Status**: accepted
