# An Account may have multiple Workspaces, with caps

v1 allows several Workspaces per Account, not one and not unlimited. A public product with empty + git Import needs more than a single slot; uncapped count and disk would exhaust the control plane and E2B.

v1 caps: **three Workspaces per Account**, **1 GiB each**.

**Status**: accepted
