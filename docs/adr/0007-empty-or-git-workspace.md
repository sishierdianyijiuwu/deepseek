# A Workspace may start empty or from git

After email verification, an Account can use an empty Workspace or import one by cloning a git URL. Empty-only would delay real projects; git-only would block people with no remote repo yet.

**Status**: accepted
