/**
 * @deepseek-ai/dsh-hosted — hosted-profile bundle. The package's substance is
 * `cordis.patch.yml`, which layers Account, mailer, auth HTTP, Account-scoped
 * Credentials, and the register/sign-in UI over dsh-base + dsh-web-app.
 * Local `dsh web` stays a separate profile without Accounts.
 * @module @deepseek-ai/dsh-hosted
 */

export {}
